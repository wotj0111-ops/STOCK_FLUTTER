name: stock_app
description: 내 주식 대시보드 (네이버 금융 크롤러 + 로컬 알림 + 뉴스 요약)
publish_to: "none"
version: 0.5.0+5

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  sqflite: ^2.3.3
  path: ^1.9.0
  path_provider: ^2.1.3
  shared_preferences: ^2.2.2
  fl_chart: ^0.68.0
  intl: ^0.19.0
  flutter_local_notifications: ^17.2.2
  workmanager: ^0.5.2
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
